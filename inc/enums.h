#ifndef ENUMS_H
#define ENUMS_H

typedef enum {
    NO_ERROR,
    FILE_NOT_EXIST,
    FILE_EMPTY,
    FILE_NON_EMPTY,
    INVALID_LINE,
    INVALID_NUMBER,
    DUPLICATED_BRIDGES,
    SUM_OF_BRIDGES,
} t_error_status;

#endif //ENUMS_H
